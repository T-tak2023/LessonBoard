<div class="mt-5 mx-4">
  <div class="card border-primary mb-3 mx-auto" style="max-width: 30rem;">
    <div class="card-header">
      <h2 class="mb-0"><i class="bi bi-pencil-square"></i> 生徒情報編集</h2>
    </div>
    <div class="card-body">
      <%= form_for @student, url: instructors_student_path(@student) do |f| %>
        <%= render 'shared/error_messages', object: @student %>

        <div class="p-3 mb-3 bg-light">
          <h4 class="card-title"><i class="bi bi-person"></i> 名前</h4>
          <%= f.text_field :student_name, class: "form-control" %>
        </div>

        <div class="p-3 mb-3 bg-light">
          <h4 class="card-title"><i class="bi bi-envelope"></i> メールアドレス</h4>
          <%= f.email_field :email, class: "form-control" %>
        </div>

        <div class="p-3 mb-3 bg-light">
          <h4 class="card-title"><i class="bi bi-book"></i> コース</h4>
          <%= f.text_field :course, class: "form-control" %>
        </div>

        <div class="p-3 mb-3 bg-light">
          <h4 class="card-title"><i class="bi bi-calendar"></i> 入会日</h4>
          <%= f.date_select :enrollment_date, class: "form-control" %>
        </div>

        <div class="d-grid gap-2">
          <%= f.submit "更新", class: "btn btn-primary" %>
        </div>
      <% end %>

      <div class="mt-3 text-center">
        <%= link_to '戻る', instructors_student_path(@student), class: "btn btn-secondary" %>
      </div>

      <div class="my-4 border-top"></div>

      <div class="mb-4 text-center">
        <%= link_to 'アカウントを削除する', instructors_student_path(@student), data: { turbo: "true", turbo_method: :delete, turbo_confirm: "生徒アカウントを削除してもよろしいですか？\n\n削除すると:\n・この生徒の全てのレッスン予定が削除されます\n・レッスンノートは保持されます\n\nこの操作は取り消せません。" }, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>
